; PHP per-directory overrides (PHP-FPM / CGI reads this automatically).
; The built-in dev server does NOT read this file — use "composer dev" which
; passes -d upload_max_filesize=25M -d post_max_size=26M to php artisan serve.
upload_max_filesize = 25M
post_max_size       = 26M
