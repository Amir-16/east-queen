// Decorative background — subtle floating leaf dots for nature feel
// Kept very lightweight so it doesn't distract from content
export default function ParticleBackground() {
  return null // Replaced by section-level SVG/gradient backgrounds
}
